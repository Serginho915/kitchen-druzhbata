services:
  frontend:
    container_name: kitchen-frontend-prod
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: runner
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
